{
  "name": "@kv-express/mailing",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "live-server src/templates",
    "build": "tsc && tsc-alias",
    "dev": "tsc && (concurrently \"tsc -w\" \"tsc-alias -w\")",
    "lint": "eslint src"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "live-server": "^1.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.4.1",
    "concurrently": "^8.2.0",
    "dotenv": "^16.3.1",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.7",
    "tsconfig-paths": "^4.1.2",
    "typescript": "^5.1.6"
  }
}
